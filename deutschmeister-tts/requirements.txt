# Piper TTS — fast, local neural TTS (ONNX/onnxruntime, no torch). The
# `piper-tts` wheel installs the `piper` package and bundles the espeak-ng
# phonemizer, and pulls onnxruntime + numpy as deps. Switched off Coqui TTS,
# which is no longer maintained and drags in a ~1-2 GB torch dependency.
piper-tts==1.4.2
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
