# API Configuration
EXPO_PUBLIC_API_URL=http://localhost:3000/api

# Push Notifications (Firebase)
# EXPO_PUBLIC_FCM_SENDER_ID=your-fcm-sender-id

# OAuth (handled via deep link scheme in app.json)
# No client secrets needed on mobile - OAuth flow uses system browser
